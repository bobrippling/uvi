change to unicode internal strings

ncurses.c
	movement:
		CTRL_AND([dufb])
		[0$wb]
	edit:
		[rcdai]
			then:
			[0-9]* repetition before switch(getch()) for ncurses


view.c
	ncurses colour


command.c:
	on failed file read (permission denied, etc), keep running

	marks

	regex search

	s/ \+$// option

	*word* = bold, _word_ = underline

	special handling for \n (e.g. with 'r')

	change variables to table:
		struct tim
		{
			const char *const name;
			void (*func)(char value);
		} tims[] = { ... };
