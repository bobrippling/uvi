replace all list_.* with buffer_.*, then store line count, etc, in the buffer

[0-9]* repetition before switch(getch()) for ncurses

'd','a' and 'i' ncurses commands

test/work with files whose len > LINES (CTRL_AND([eydufb]))

marks

regex search

s/ \+$// option

*word* = bold, _word_ = underline

special handling for \n (e.g. with 'r')
