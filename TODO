ncurses.c:
	only syntax highlight what's on screen?
	'J'
	%ddd bug (FIXME'd)
	:e autocomplete?

	change tabs -
		int ntabs = curpos / 8;
		mov(ntabs * 8 + 8)

	fix:
		>free(b->fname);
		>if(s){ <-- cursor here: "d%"
		>	b->fname = umalloc(strlen(s) + 1);
		>	strcpy(b->fname, s);
		>}else
		>	b->fname = NULL;
		ends up with if(s) deleted

motions.c:
	'f' and 't'

command.c:
	filters/pipes:
		:r!cmd
		:w!cmd
		:rw!cmd # filter through prog
	s/ \+$// command
	regex search

regex:
	simple (vi(1) - see '/'): ^$.[]*
