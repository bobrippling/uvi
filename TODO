change to unicode internal strings

sort out tabs - check nano/use gnu tabstop thing

view.c
	ncurses colour

ncurses.c:
	'J'
	'K'

command.c:
	regex search

	s/ \+$// option

	*word* = bold, _word_ = underline
