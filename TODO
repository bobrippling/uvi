change to unicode internal strings

ncurses.c
	movement:
		CTRL_AND([dufb])
	edit:
		r[eplace]
			then:
			[0-9]* repetition before switch(getch()) for ncurses
			special handling for \n (e.g. with 'r')


view.c
	ncurses colour


command.c:
	marks

	regex search

	s/ \+$// option

	*word* = bold, _word_ = underline

	check fclose() and writev() return values
