ncurses.c:
	only syntax highlight what's on screen?

	'f' and 't'
	'J'
	%ddd bug (FIXME'd)
	:e autocomplete?

	fix motions/get rid of hacks
	move marks to motion.c

command.c:
	s/ \+$// option
	regex search

regex:
	simple (man vi - see '/'): ^$.[]*
