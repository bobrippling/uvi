change to unicode internal strings

view.c
	ncurses colour

ncurses.c:
	'J'
	'K'

command.c:
	investigate binary (command.o) file read

	regex search

	s/ \+$// option

	*word* = bold, _word_ = underline
