ncurses.c:
	bug: on newline tabs get displayed as the terminal pleases
	bug: on backspace, if there was a tab the cursor shifts all over

	change 'w' and 'b' to toggle between /\s/ and !/\s/ like gui textboxes

	change tabs - access termios?
		int ntabs = curpos / 8;
		mov(ntabs * 8 + 8)

	only syntax highlight what's on screen?
	:e autocomplete?

	'u' (aka undo)

	SIGWINCH - handle

command.c:
	add range option to filters/pipes
	:x - :wq, but only write if changed
	use $SHELL in shellout()

regex:
	simple (vi(1) - see '/'): ^$.[]*
